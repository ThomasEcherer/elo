Ext.data.JsonP.core_03_localization({"guide":"<h1 id='core_03_localization-section-localization'>Localization</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ul>\n<li>1. <a href='#!/guide/core_03_localization-section-properties-files'>Properties files</a>\n </li>\n<li>2. <a href='#!/guide/core_03_localization-section-in-elo-apps'>In ELO Apps</a>\n </li>\n<li>3. <a href='#!/guide/core_03_localization-section-in-elo-modules'>In ELO Modules</a>\n </li>\n<li>4. <a href='#!/guide/core_03_localization-section-conflicts'>Conflicts</a>\n </li>\n</ul></div>\n\n<p>This guide explains the possibilities to localize ELO Apps and modules.</p>\n\n<h2 id='core_03_localization-section-properties-files'>Properties files</h2>\n\n<p>ELO Apps provide a <em>locales</em> folder that contains a properties file. Properties\nfiles are used in the Java Programming language. Basically a Properties file is\na text file containing a list of key-value pairs. Each pair is stored in one line,\nthe key is separated from the value by <em>=</em>. For each language a separate file is\ncreated using the ISO 639 two letter language (e.g. myApp.properties -> myApp_fr.properties).\nThe default file is expected to contain the text in the language given by the\n<em>defaultLanguage</em> attribute of the ELO App.</p>\n\n<p><strong>Please note</strong>: All properties files are stored with UTF-8 encoding.</p>\n\n<p><strong>Please note</strong>: Only the two letter <em>language</em> for the properties files are supported\nnot additional <em>country</em> or <em>variant</em> specific files.</p>\n\n<h2 id='core_03_localization-section-in-elo-apps'>In ELO Apps</h2>\n\n<p>In all ELO Apps a properties file for the <em>defaultLanguage</em> of the app get created.\nThe files name is the id of the ELO App. This file already holds the display name\nof the ELO App. Changes on the properties file needs a rebuild of the app in the\nELO App Manager in order to build the JavaScript locale files.</p>\n\n<p>An ELO App chooses its language on load by the following order:\n- If there is a url parameter <strong>lang</strong> given the two letter language is used from its value.\n- Else the language of the request locale header is used.</p>\n\n<p>The language will be evaluated if it is supported, i.e. if there is a properties\nfile for that language. If not the <em>defaultLanguage</em> is used.</p>\n\n<p>Typically the language setting comes from:\n- The chosen language in the apps login screen\n- The client language if the app is embedded inside a client</p>\n\n<h3 id='core_03_localization-section-html'>HTML</h3>\n\n<p>In the static html snippet file localized text can be added using <strong>${key}</strong>,\nwhere key is a key in the properties file. When the app gets loaded the html\nsnippet will be parsed and all occurences will be replaced by the requested\nlanguages text.</p>\n\n<h3 id='core_03_localization-section-javascript'>JavaScript</h3>\n\n<p>For JavaScript the properties files get rewritten to according JavaScript files\nholding an JavaScript object containing the localized text. Only the script with\nthe requested language gets loaded in the app, so accessing other languages on\nruntime is not possible.</p>\n\n<p>To get a localized text from a given key the api.helpers.Text method can be used.\nHere also dynamic values can be placed in predifined gaps in the localized text.\n(E.g. if <em>APP.WELCOME=Welcome {0}</em> is one pair in the properties file:</p>\n\n<pre><code>var text = api.helpers.Text.getText('APP.WELCOME', 'Ben');\n// text is Welcome Ben\n</code></pre>\n\n<h3 id='core_03_localization-section-angularjs-templates'>AngularJS templates</h3>\n\n<p>In order to use the localized text in AngularJS templates, the AngularJS Filter\n<strong>eloLocales</strong> can be used from the elo.module.components module.</p>\n\n<pre><code>&lt;h1&gt;{{ \"APP.WELCOME\" | eloLocales:userName }}&lt;/h1&gt;\n</code></pre>\n\n<p>Where userName is a scope variable holding the string 'Ben' from the example above.\nIf no dynamic key is used <em>:userName</em> can be omitted.</p>\n\n<h2 id='core_03_localization-section-in-elo-modules'>In ELO Modules</h2>\n\n<p>ELO modules by default do not have a locales folder with an properties file created.\nIf the <em>defaultLanguage</em> of a module is set in the manifest a locales folder is\nexpected and the module the build process will try to create JavaScript locale files.\nThe properties file will have the name of the modules id in camel case notation\n(dots are replaced).</p>\n\n<p><strong>Please note</strong>: If the <em>defaultLanague</em> of a module is set, the locales folder\nis expected to be existing and containing at least the default properties file.</p>\n\n<h2 id='core_03_localization-section-conflicts'>Conflicts</h2>\n\n<p><strong>Please note</strong>: In order to identify the correct text inside <strong>all</strong> loaded\nproperties JavaScript files, the keys has to be unique in <strong>all</strong> files.\nIt is suggested to use <strong>APP.</strong> prefix for all keys of the ELO Apps properties file.\nAnd for each module a separate prefix.</p>\n","title":"Localization"});